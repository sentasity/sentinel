---
title: Architecture reference
description: The receiver, its routes, the investigation and autofix pipelines, and the infrastructure behind them.
---

The engine is one Lambda (the receiver), one DynamoDB table, a one-minute EventBridge schedule, and one kind of unattended Claude Code session: a cloud routine that investigates a batch of issues and, for the findings the receiver's gate approves, writes the fix as well. This document walks the pipeline end to end and then covers the security model that makes the unattended parts safe to run.

## The receiver

`receiver/` is a single Lambda behind a Function URL. `handler.py` routes:

| Route | Caller | Purpose |
|---|---|---|
| `POST /sentry` | Sentry | Issue alert webhook, HMAC-verified |
| `POST /bot` | Azure Bot Service | Teams bot messages (JWT-verified) |
| `POST /findings` | The investigator session | Findings document for a fired batch; the response vends any autofix grants it earned |
| `POST /findings/probe` | The probe session | Configuration probe report |
| `POST /autofix-result` | The same session's fix phase | Outcome callback, capability-token auth |
| `GET /health` | Operators | Liveness |

The same Lambda is also invoked directly (not over the URL) by the EventBridge rule that drives the sweep, so the scheduled path is not reachable from the internet.

State lives in one DynamoDB table (`receiver/store.py`): posted alert cards and their Teams message ids, the pending-investigation queue, dedupe and skip caches, autofix dispatches and their callback tokens.

## Alert to Teams card

`POST /sentry` verifies the webhook signature, parses the alert (`models.py`), renders an Adaptive Card (`cards.py`), and posts it to the configured Teams channel through the receiver's own bot identity (`bot.py`, an Entra app registration plus an Azure Bot). Posting directly through a bot identity means message ids come back synchronously, which is what makes threaded follow-up replies (findings, autofix outcomes) possible.

## The investigation pipeline

**Gate** (`investigation.py`). An alert is worth investigating when it is error-level, in an investigated environment, and carries a release that resolves to a commit SHA (for projects where the release *is* the SHA, a 40-char hex check; anything else means something changed upstream and the alert is skipped rather than investigated at branch HEAD). Eligible alerts are enqueued with a debounce so that a burst of related issues becomes one batch.

**Sweep** (`sweep.py`). Every minute, the sweep groups due pending issues per project and release (up to `max_batch_issues`), respects the per-sweep and daily fire caps, and fires one routine session per batch. It also enforces deadlines: a batch whose findings never arrive within `deadline_seconds` is failed loudly into the thread rather than left hanging.

**Fire** (`routines.py`). A fire is `POST /v1/claude_code/routines/{id}/fire` with a JSON payload of exactly five fields: `project`, `issue_ids`, `release`, `batch_id`, `reply_token`. The payload is capped at 65,536 characters by the API, which is why the engine sends issue ids only and lets the session fetch event detail itself. The API has no idempotency key, so the receiver owns dedupe. On 429, `Retry-After` decides the response: a short delay is read as the daily routine-run cap and earns one bounded retry; a long one is read as subscription-window exhaustion and is skipped and logged (usage credits are off, so exhaustion rejects rather than bills).

**Investigate** (`prompts/investigator.md`). The session checks out the target repo at the release SHA, investigates each issue in the batch, and posts a findings document to the receiver's `/findings` endpoint, authenticated by the `reply_token` minted for that batch.

**Findings** (`findings.py`). The receiver validates the document shape, redacts secret-shaped strings, renders a reply card, and posts it into the original alert's thread.

## The autofix pipeline

Autofix runs inside the investigation session, not after it. The reason is a property of the runtime rather than a preference: the receiver can answer a session's request, but it cannot send a message to a session already running. The HTTP response to `POST /findings` is therefore the only channel it has, and everything the fix phase needs has to travel back down it.

**Gate** (`autofix.py`). Evaluated per result as the findings are delivered. A finding proceeds to autofix when autofix is enabled, the project is on the allowlist (an empty allowlist opts in every project), the findings document is schema v2 or later, the result is `investigated`, its confidence and fixability each clear the configured minimums, no cited file matches a forbidden or excluded path, no fix has already been attempted for this issue in this environment on this release, and the daily pull-request cap has budget left. Checks run cheapest first and the first failure wins, so a decline names one reason and appends it to that finding's reply card. The single exception is autofix being switched off entirely, which says nothing at all: with the kill switch off, no thread carries an autofix line.

**Grant.** A passing finding stages a grant: a dispatch id, a one-time callback token (stored hashed, scoped to that single dispatch), and the files the finding cited. Grants are staged rather than posted immediately, because the credential they promise does not exist yet.

**Mint** (`github_app.py`). Once the whole batch is gated, the receiver mints one GitHub App installation token for `target_repo`, downscoped to that one repository and to `contents: write` plus `pull_requests: write`. GitHub gives it one hour. If minting fails, every staged grant is withdrawn and its reply line is rewritten before any thread reads it, so a thread never promises a fix that has no credential to start with.

**Vend.** The `/findings` response then carries the repository, the base branch, the token and its expiry, the callback URL, and one entry per approved grant. Reply cards post last, after the mint has resolved.

**Fix** (`prompts/investigator.md`, step 7). The same session works each grant independently: check out the base branch through a remote authenticated with the vended token only, diff the cited files against the release it investigated and abort if that drift invalidates the diagnosis, re-verify the root cause at this checkout, write the fix and a test that fails without it, run the narrowest relevant test command, then push a branch named after the short id and the dispatch id and open a pull request against the base branch. Every path through that phase reports to `/autofix-result` exactly once, including the paths that stop early.

**Verify and close.** On a `pr_opened` callback the receiver checks that the pull request lives in `target_repo` and was authored by the App's own bot login. Verified, the outcome posts into the Teams thread; unverified, the thread gets a warning instead and the log carries an authorship-mismatch marker. A grant that never calls back is failed by the sweep once its hour is up, so a session that dies mid-fix still closes its thread. Every pull request waits for a human reviewer; nothing merges automatically.


## Security model

The trust boundary and every control around the unattended sessions have their own
page: [Security model](/sentinel/security-model/).

## Infrastructure

`infra/` is a self-contained CDK v2 app deploying one stack: the receiver Lambda and Function URL, the DynamoDB table (retained on stack delete), the EventBridge sweep rule, and the wiring to the SSM parameters. Deploy instructions are in [infra/README.md](https://github.com/sentasity/sentinel/blob/main/infra/README.md).
