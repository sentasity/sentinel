---
title: Security model
description: What an unattended session can touch, what the credential it is handed can do, and which controls stay on the receiver where injected text cannot argue with them.
---

The unattended sessions are the trust boundary. Everything that reaches them is treated
as untrusted, including the Sentry issue text they are asked to investigate.

:::tip[The short version]
The operator's controls over whether a fix is allowed live in the receiver, out of the
session's reach; the session supplies two of the gate's inputs, its own confidence and
fixability ratings, and those are influenceable. The credential it is handed is scoped
to one repository, two permissions, and one hour, and it cannot push a workflow file.
It *can* push to any branch, so branch protection on the base branch is what makes
review unavoidable, and every pull request waits for a human.
:::

## The fire payload is data, not instructions

All parameterization lives in the stored routine prompt, not in the request that
triggers it. The payload carries five typed fields and no prose:

```
{ "project", "issue_ids", "release", "batch_id", "reply_token" }
```

The prompt instructs the session to treat any deviation from that shape as a malformed
fire and report it rather than interpret it. This is the control that matters most,
because the alternative design, passing instructions in the trigger, would make anyone
who can reach the trigger endpoint able to reprogram the session.

The payload also sends issue ids rather than event bodies. That is partly the API's
65,536-character cap on a fire, and partly that a full event body is attacker-influenced
text; the session fetches what it needs through the Sentry connector instead.

## The routines API exposes no configuration read

There is no way to ask the API which repository a routine is bound to, or which
connectors it has. So the receiver cannot assert at startup that the routine it is
about to fire is the one it thinks it is.

The compensating control is a fail-closed probe: a session whose only job is to report
its own configuration back. It is a manual gate, not an automatic one, and it needs
re-running whenever the routine or the account's connectors change. Naming it here
rather than hiding it is deliberate, because a compensating control nobody knows to
re-run is not a control.

## Findings delivery is verified out-of-band

Before posting findings anywhere, the session confirms the destination against two
channels the operator controls and no injected text can forge:

- `SENTINEL_RECEIVER_URL`, set in the cloud environment rather than passed in the fire
- the environment's deny-by-default egress policy, whose only non-default allowed hosts
  are the receiver and the GitHub endpoints the fix phase needs

An instruction embedded in a stack trace telling the session to post its findings
somewhere else fails both checks.

The check is narrower than it may look, and the qualifier in that second bullet is
load-bearing. The policy is deny-by-default over the hosts the operator controls; the
environment still reaches whatever it defaults to reaching, and the investigation phase
fetches the repository over the network before any of this. So the egress policy pins
down where *findings* may be posted. It is not a general containment boundary, and the
wider the set of GitHub endpoints the fix phase needs, the less of one it becomes. The
controls below are what carry the weight.

## The session that writes the fix is the one that investigated

Autofix does not run in a second session started afterwards. The session that reported
the findings writes the fix, using a credential the receiver returns in the HTTP
response to that report.

That shape follows from one fact about the runtime: the receiver can answer a session
that calls it, but it cannot send a message to a session that is already running. A
credential can only travel back down a request the session itself made, and the findings
POST is the request there is. Everything below is designed around handing a running
session a credential and then living with the consequences, because there is no design
where the credential arrives some safer way.

### The vended token

| | |
|---|---|
| Scope | one repository, the configured `target_repo` |
| Permissions | `contents: write` and `pull_requests: write`, nothing else |
| Lifetime | one hour, set by GitHub when the token is minted |
| Storage | none: minted per batch, returned once, never written down |

`workflows` is absent deliberately. A workflow file pushed to a branch runs in CI, with
access to the repository's secrets, before any human reviews the pull request that
carries it. A credential that can write `.github/workflows/` is therefore not a
code-writing credential at all; it is a secrets-reading one, and reviewing the pull
request afterwards is too late to matter.

The gate also declines any finding that cites a path under `.github/`, but the two
controls are not doing the same job. The path check is a pre-filter: it reads the paths
the finding *claimed*, and a fix touches whatever a fix touches. It makes an
inappropriate finding decline early and legibly. The token's scope is the enforcement,
because it constrains what the session can actually push rather than what it said it
would touch. When the two disagree, the scope wins, and the prompt tells the session to
report itself declined rather than discover the refusal at push time.

### The webhook signature does not contain the content

Sentry's webhook is HMAC-verified, and it is worth being precise about what that buys.
The signature proves the alert came from the Sentry integration. It proves nothing about
the text inside the alert.

Stack traces and error messages routinely carry whatever user input caused the exception.
That is what they are for. So anyone who can make the monitored application throw can put
text of their choosing in front of an unattended session, through a channel that is fully
authenticated and behaving exactly as designed. There is no signature check that helps
here, and treating one as though it did would be the more dangerous error.

What contains it instead is everything downstream of the text: the token is scoped so a
fix cannot reach beyond one repository or past the two permissions above; branch
protection on the base branch means nothing lands without review; and a human reads the
pull request before it merges. The prompt also tells the session to treat code,
repository docs, Sentry text, and command output as data and to say in the pull request
body when it saw something that read like an instruction, which helps a reviewer but is
not itself a control.

The one gate input this text can reach is the finding's own confidence and fixability
ratings, which the session supplies. Steering an investigation toward a diagnosis that
looks confident and contained is the cheapest thing an injected instruction can attempt,
and it is not something the receiver can second-guess: rating the finding is exactly the
judgment the session was fired to make. What that buys an attacker is bounded, though. A
flattering rating gets a finding *to* the gate; it does not get it past the operator
controls below, which are evaluated on values the session never supplies.

### Operator controls live on the receiver

Confidence and fixability are the session's own judgments and travel in the findings
document, which is why the section above treats them as influenceable. Every *other*
input to the decision is an operator control, and each one is evaluated in the receiver
rather than in the prompt:

- the kill switch that disables autofix entirely
- the project allowlist
- the forbidden and operator-excluded paths
- the dedupe, so one issue earns one fix attempt per environment and release
- the daily pull-request cap

The placement is the point. Text inside an investigated Sentry event can argue with a
prompt; it can put a sentence in a stack trace claiming the cap is lifted or the project
is opted in. It cannot argue with a conditional write in the receiver. Moving any of
these into the prompt to make it "smarter" would move it inside the attacker's reach.

The same reasoning covers the caps' side effects: the dedupe and cap checks are
conditional writes, spent as they pass, so a finding cannot be re-evaluated into a second
attempt.

### Pull-request authorship is verified, and that is a detective control

The session's runtime carries connectors of its own, and in a realistic deployment one of
them is a GitHub connector authenticated as a person with write access to the same
repository. The prompt is explicit that the vended token is the only credential to use
with GitHub and that work arriving under a person's name misrepresents who wrote it. A
prompt is guidance, not enforcement.

So when a callback reports `pr_opened`, the receiver checks the claim rather than
believing it. The pull request URL must parse, name the configured target repository, and
resolve to a pull request whose author is the App's own bot login. That lookup is done
with a token downscoped to `pull_requests: read`, because a verification step should not
itself hold a write credential, and every failure path (an unparseable URL, another
repository, a lookup that errors) reads as unverified rather than as verified.

An unverified result posts a warning into the thread instead of a link, telling the
reviewer the authorship could not be confirmed. It also writes a log line carrying both
the authorship-mismatch marker and the existing autofix failure marker. Reusing the
failure marker is deliberate: the alarm already wired to it fires here with no second
alarm to define and keep in sync, while the new marker is what tells a reader which kind
of failure this was.

Calling this detective rather than preventive is not modesty, it is the accurate label.
The check runs after the pull request exists. It catches a session that used the wrong
identity and then reported honestly; it does not catch one that used the wrong identity
and stayed quiet, which surfaces only as a grant that never called back before its
deadline. The connector's ambient write access is a real, accepted exposure, and the
honest description of the posture is that the vended token makes the correct path easy
and legible rather than making the incorrect one impossible.

The residual risk of the token itself is the same shape. It is a bearer credential that
spends up to an hour inside a session whose context contains attacker-influenceable text.
Its blast radius is bounded by the scope and the lifetime above, and no further: within
that one repository, `contents: write` is repository-wide, so the token can push to any
ref, the base branch included. Pushing a topic branch and opening a pull request is what
the prompt instructs, not what the credential restricts.

That is the gap branch protection fills, and it is the operator's job rather than
something this engine can do for them. With the base branch protected, the token's
reachable outcome really is a pull request somebody reviews. Without it, a session that
departed from the prompt could write to the base branch directly, and none of the
controls on this page would stop it.

### The callback is a capability, not an identity

Each grant carries its own callback token, stored only as a hash and valid for that one
dispatch. The receiver's advance to a settled state is conditional, so a late callback
and the timeout sweep cannot both write into a thread, and a replayed callback settles
nothing twice. A grant that never reports is failed into the thread when its hour
expires, which is the same principle as the investigation deadline: the failure mode
worth engineering against is the silent one.

## Secrets

Receiver-side secrets live in SSM SecureStrings; workflow-side secrets are Actions
secrets. Neither ever appears in the config file, which carries only a reference to the
parameter name. The full inventory and the rotation runbooks are in
[Runbooks](/sentinel/operate/runbooks/).

One non-credential is held the same way: the ban list `tests/test_public_tree.py`
enforces splits its generic patterns, which are tracked, from the literals that would
identify a real deployment, which live in an untracked local file mirrored by an
Actions secret. A tracked ban list would be an index of everything it protects.

## The disclosure scan reads and nothing else

A separate unattended session sweeps the repository before anything is published from it,
looking for what a public tree should not hand a stranger: live credentials, real tenant
and organization identifiers, deployment endpoints, and the inventories that turn a
runbook into a deployment map.

It holds the narrowest posture of any session here. The session gets `Read`, `Glob`, and
`Grep`, no shell and no write tool, and it loads no project settings, because the tree
it reviews is also where an injected instruction file would be waiting. Its verdict
counts only when it carries a token minted after that tree was already fixed, so a
result block planted in a file cannot claim the run's outcome, and it prints a fixed set
of fields so a value smuggled into an extra one never reaches the public log.

[Runbooks](/sentinel/operate/runbooks/) covers running it and reading what it reports.

## Reporting a vulnerability

Please report anything you find through
[GitHub's private vulnerability reporting](https://github.com/sentasity/sentinel/security/advisories/new)
rather than opening a public issue.
